<% if @presenter.past_conditions.count > 0 %>
<div class="row">
  <div class="col-md-12">
    <div class="row">
      <div class="col-md-12">
        <h1>Past Conditions</h1>
        <table class="table">
          <thead class="posts_head">
            <tr>
              <th>
                Code
              </th>
              <th>
                Description
              </th>
              <th>
                Additional
              </th>
              <th>
                Reported
              </th>
              <th>
                Cured
              </th>
              <th>
                Notes
              </th>
              <th>
                Reporting Doctor
              </th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody class="posts_body">
            <% @presenter.past_conditions.each do |condition| %>
            <tr>
              <td><%= condition.icd10.code %></td>
              <td><%= condition.icd10.description %></td>
              <td><%= condition.icd10.additional %></td>
              <td><%= condition.reported %></td>
              <td><%= condition.cured %></td>
              <td><%= condition.notes %></td>
              <td><%= condition.doctor.first_name + " " + condition.doctor.last_name %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
<% end %>
