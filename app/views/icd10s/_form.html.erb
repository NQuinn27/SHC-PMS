<%= simple_form_for @icd10, html: { class: 'form-horizontal' },
  wrapper: :horizontal_form,
  wrapper_mappings: {
    check_boxes: :horizontal_radio_and_checkboxes,
    radio_buttons: :horizontal_radio_and_checkboxes,
    file: :horizontal_file_input,
    boolean: :horizontal_boolean,
    date: :horizontal_form
  } do |f| %>
  <% if @icd10.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(icd10.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
      <% @icd10.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :code %>
  <%= f.input :description %>
  <%= f.input :additional %>
  <%= f.button :submit %>
  <br>

<% end %>
